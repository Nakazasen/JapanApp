This is a large and complex project for learning languages, likely English and Japanese. It has a frontend, a backend, a database, and various scripts for data processing.

**Instructions for Gemini:**

*   **Project Structure:**
    *   `frontend/`: The user interface. It could be a web framework like React or Vue, or a desktop framework like PyQt/PySide.
    *   `backend/`: The application server. It's likely a Python-based framework like FastAPI or Flask.
    *   `db/`: Database related files, possibly migrations or schema definitions.
    *   `data/`: Raw data files, like Excel sheets (`.xlsx`) or PDFs.
    *   `scripts/`: Various utility scripts for data processing and maintenance.
    *   `run.py`: The main entry point to start the application.
    *   `requirements.txt`: The Python dependencies.

*   **Key Files:**
    *   `README.md`: Provides a general overview of the project. Read this first.
    *   `QUICK_START.md`: Contains instructions on how to get the project running.
    *   `config.yaml`: The main configuration file.
    *   `run.py`: The entry point to run the application.

*   **Your Role:**
    *   You are an assistant for developing and maintaining this language learning application.
    *   Help with understanding the complex codebase, which includes a frontend, backend, and data processing pipelines.
    *   Assist with debugging issues across the entire stack.
    *   Help with adding new features, which may involve modifying the database, backend APIs, and frontend components.
    *   When working on data import scripts (like those for `.xlsx` or PDF files), be careful and validate the data transformations.
    *   Follow the instructions in `QUICK_START.md` to set up and run the development environment.
